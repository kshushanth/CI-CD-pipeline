name: CI/CD Pipeline
on:
push:
 branch:[main]
workflow-dispatch:
jobs:
 build:
  runs-on: ubuntu-latest
steps:
 -name: Checkout code
  uses:action/checkout@v4
 -name: Set up Node.js
  uses: actions/setup-node@v4
  with:
   node-version:'18'
 -name: install dependancies
  run:npm install
 -name: Bulid application
  run:echo"this is application"
test:
    runs-on: ubuntu-latest:
    need: Bulid
    steps:
    -name: Checkout code
     uses: action/checkout@v4
    -name: install dependancies
     run: npm install
    -name: Run test
     run: npm test
deploy:
 runs-on: ubuntu-latest:
  need: Test
  steps:
  -name: Checkout code
   uses: action/checkout@v4
  -name: Deploy the application
    run: echo"this is Deploy the application
